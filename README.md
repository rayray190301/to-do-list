 To-Do List - Lista de Tarefas Dinâmica

Este projeto representa o "Hello World" do Desenvolvimento Web Front-end. É uma aplicação simples, mas completa, que demonstra o ciclo de vida de dados no navegador.

Funcionalidades

- Adicionar Tarefas: Insere novos itens na lista através do formulário.
- Marcar/Desmarcar: Risca visualmente a tarefa concluída.
- Exclusão: Permite remover itens da lista permanentemente.
- Persistência (Local Storage): As tarefas são salvas na memória do seu navegador e não somem ao atualizar a página.

 🛠️ Tecnologias e Aprendizados

Este projeto foi construído puramente com tecnologias web de base, focando nos seguintes conceitos:

HTML5: Estrutura semântica do formulário e da lista.
CSS3: Estilização, layout Flexbox e classes condicionais.
JavaScript: Lógica de aplicação, manipulação do DOM (Document Object Model) e eventos.
Local Storage: Armazenamento de estado (Gerenciamento de Dados).

Organização do Código (Boas Práticas)

O código está dividido de acordo com a boa prática de modularidade para facilitar a leitura e manutenção:

1.  `index.html`: Apenas a estrutura (o "esqueleto").
2.  `style.css`: Apenas a estilização (a "aparência").
3.  `script.js`: Apenas a lógica e interação (o "cérebro").

⚠️ Aviso: O arquivo que contém todos os blocos de código integrados é apenas para referência e estudo inicial. Para rodar o projeto, use os arquivos separados.

⚙️ Como Executar

Para ver a aplicação rodando, siga estes passos simples:

1.  Clone o Repositório:
    ```bash
    git clone [SUA_URL_DO_REPOSITÓRIO]
    ```
2.  Navegue até a pasta:
    ```bash
    cd to-do-list
    ```
3.  Abra no Navegador:
    Localize e abra o arquivo `index.html` diretamente no seu navegador de preferência (Chrome, Firefox, etc.).

✒️ Rayssa Hugo.

